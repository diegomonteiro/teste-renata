services:
  python-app:
    build: .
    container_name: laz_web_processor
    restart: unless-stopped
    volumes:
      - .:/app
    ports:
      - "9090:9090"
    environment:
      - PYTHONUNBUFFERED=1
      - APP_ENV=production
